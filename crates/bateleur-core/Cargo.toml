[package]
name = "bateleur-core"
version = "0.20.1"
edition = "2021"
description = "Shared mail, cache, and staff types. Desktop and future mobile shells depend on this crate — not on a vendor mail API."
authors = ["Kipkirui"]
license = "MIT"
repository = "https://github.com/kipkirui/Bateleur"

[dependencies]
serde = { version = "1", features = ["derive"] }
